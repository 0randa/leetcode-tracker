# Private server-only env. The browser never sees this — only the SvelteKit
# server reads it (BFF proxy). Set the same value in Vercel project env vars.
SPRING_API_BASE=https://lctracker-backend.onrender.com
